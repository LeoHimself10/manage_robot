# 马荣鑫确认弹窗按钮对齐

last_verified_at: 2026-09-16
release: quality-test-dialog-20260916-r11

FR-QS-06：确认进入质量事件时，取消与确认按钮同一行、等高、右对齐，取消在左，蓝色确认在右。

修复 `public/quality/ma-workbench/index.html` 的共享 info 弹窗：将入队确认按钮从正文移入底部操作区；每次打开时重置按钮与关闭文案，避免上次确认操作残留。实际 OA 与演示确认入口均复用该弹窗。

无 API、状态机、迁移或审计字段变更，原按钮事件委托保持不变。

验证：1366×768 和 1920×1080 浏览器运行实际 confirmIntake 入口，按钮顶边与高度一致；取消关闭、重新打开普通提示无残留按钮，控制台无脚本错误。发行 534 文件哈希校验通过。

部署：沿用质量测试服务 8092 的配置和数据卷，发布前备份 SQLite，重启后验证原任务、事件、快照、初析、证据与审计记录保留。回滚镜像为 `manage-robot:quality-test-simplify-20260916-r10`。
