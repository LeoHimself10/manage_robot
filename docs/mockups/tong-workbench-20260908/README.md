---
status: interactive-prototype
last_verified_at: 2026-09-10
scope: tong-quality-management-view
runtime: local-real-ai-with-sample-business-data
---

# 佟成质量处理工作台 · 2026-09-08

## 2026-09-09 视角切换入口修正

新版菜单只保留一组马荣鑫/佟成质量视角，移除误导到旧版的重复链接。原系统管理员切换后的质量首页也通过本地导航桥回到 8808/8809。实现位于相邻 `yesterday-admin-test-actors` 工作树的 `src/web/quality-local-review-entry.ts`、`src/web/quality-http.ts`，其本地启动器启用 `QUALITY_LOCAL_REVIEW_UI_ENABLED=1`。已有会话、原权限检查、数据库、业务接口、任务页与原记录深链保留；这次只统一页面入口，不合并两个界面的数据存储。

原服务以 `--keep-data` 重载，真实 AI 配置保留。`npx vitest run tests/web/quality-local-review-entry.test.ts tests/web/quality-role-panels-http.test.ts` 在原服务工作树中通过 18 项测试；覆盖有效身份跳转、权限拒绝、伪造视角参数、部署默认关闭、原记录深链与任务视图保留。

## 2026-09-09 初析工作区去重

- 默认主界面只显示一份“质量初析草稿”，生成按钮保留；完整 AI 原稿、版本选择、生成历史和调用信息集中在“查看 AI 原稿 / 历史版本”中，默认折叠。
- 最新成功的 AI 建议与草稿基准分别标识。新建议不会自动覆盖现有草稿；“对照新建议”仅展开参考，“用新建议预填草稿”沿用确认弹窗，替换前保存人工历史副本。部门候选跟随草稿所依据的 AI 版本，避免混用新旧建议。
- 来源摘要收为“来源描述 · 供核对”，展开后仍可整理摘要；原始 OA 快照只读。“已核实的事实”保留为主编辑区，并提示人工核对预填内容。两个原有字段分别保存，不做数据合并或删除。
- 生成中和失败提示留在工作区顶部，不会因原稿折叠而隐藏。保存草稿、成果选择、正式确认说明与提交校验保留；隐藏的必填项遗漏时自动展开。
- 本轮仅改 8809 的本地前端表现，仍使用原系统 AI 接口；没有修改 OA、任务或正式业务后端。修改前文件保存在 `revisions/before-initial-analysis-simplification-20260909/`。
- 验证：`node scripts/quality-initial-workspace-check.cjs`，8 项场景通过；覆盖折叠、版本对照、取消/确认预填、历史快照不变、保存刷新、生成期间编辑、失败与必填校验。1366×768 / 1920×1080 截图见 `qa/initial-workspace-20260909/`。测试使用独立浏览器存储及 AI 响应替身，未消耗真实模型额度。

沿用马荣鑫原型样式，并实际对照 8797 本地原系统的佟成测试身份、质量初析、必须提交成果、责任结构与证据、终验与审计页面。**2026-09-09 已接入原系统真实 AI 模块**，详见 [AI 接入与验收](../quality-connected-20260909/README.md)。事件、部门、任务及 OA 评论仍为本地样例交互；下方较早日期的模拟说明是历史实现记录。

入口：<http://127.0.0.1:8809/>。请在本工作树根目录运行（同时启动 8808 / 8809）：

```powershell
node --import tsx scripts/quality-ui-ai-server.mjs
```

## 风格与功能来源

- `reference-base.css`：从 `docs/mockups/oa-feedback-inbox-20260908/ma-workbench/index.html` 的样式复制。
- `reference-workbench.css`：从同目录的 `workbench.css` 复制。
- `source-data.js` 和 `assets/`：复制同一原型的演示 OA 快照与附件，未读取真实业务数据。
- 新交互：`index.html`、`tong.css`、`tong.js`、`ai-initial-analysis.css`、`ai-initial-analysis.js`、`acceptance.css`、`acceptance.js`。样式快照独立保存，参考原型后续编辑不会影响本页。
- 原系统核对：`/workbench/quality?eventId=quality-test-event-extra-025&testActor=quality-management`；实际查看了质量初析、必须提交成果、分配投影、任务证据、终验操作。
- 领域边界核对：`src/web/quality-tracking-page.ts`、`src/quality/closure/quality-closure-service.ts`，以及质量追踪基线 01–05 和 AGENTS 目标基线。

## FR 与验收标准

- FR-TONG-01：统一员工视角，显示“质量管理能力已开通”；按待初析、跟踪、待终验、退回重开、关闭及全部事件筛选。选中行下全宽展开，始终只展开一个事件；阶段切换不移除流程导航。
- FR-TONG-02：原始来源、AI 原建议、马荣鑫结论、佟成初析分层展示。AI 快照显示来源/规则/案例/模型提示词版本，不使用未经校准的准确率。
- FR-TONG-03：主动生成 AI 初析，原稿与人工草稿分开保存；包含问题方向、主管分类只读引用、来源摘要、已确认事实、分析依据、初步结论、原因假设、待调查方向、信息缺口、处理要求、部门候选、建议期限与必须成果候选。草稿允许不完整保存；正式确认必须选择主责部门、至少一项完整成果并填写确认说明。来源更新需核对；完成版本不覆盖。
- FR-TONG-04：分配后修订保留 V1/V2/V3。任务要求、成果或建议期限变化时标记待主管同步；同步前正式任务投影保持原值。主管确认同步的演示会恢复受影响任务执行及重新验收。
- FR-TONG-05：主管根卡、并行任务折叠卡、任务内证据。查看承接、进度、提交与主管验收。证据归属于具体任务，按版本预览、下载。人员、改派、改期与主管验收不在质量页提供业务编辑入口。
- FR-TONG-06：验收仅分“验收通过 / 验收不通过”。通过仅填写一段验收意见，提交后关闭事件并在原 OA 单据的评论区展示同步结果；正式任务验收、叶子证据及待同步变更仍由页面数据自动校验。不通过时选择主管或员工任务节点，并填写一段原因与补充要求；退回主管时由主管继续组织处理，退回员工时只恢复对应任务。关闭后只读，重开沿用节点选择，全部历史结果、证据、验收与评论保留。
- FR-TONG-07：补充情况独立留存；通知失败可重新入队。浏览器刷新保留演示进度；支持确认后重置演示状态。
- FR-TONG-08：1366×768、1920×1080 下无页面横向溢出；正文输入 14px，主要输入高度至少 42px，长表单全宽展示。

## 演示情境

- a4：安装完成后参数校验提示异常，待填写初析。
- a3：连接导管后未识别，已有初析草稿，来源 V2 待核对。
- a8：图像导出时间较长，V1/V2 初析、三项任务及版本证据齐全，待终验。
- a5：包装标签与外箱记录不一致，一项验收通过、一项退回补充、一项待承接，并展示超期。
- a11：间歇提示音，初析完成，待主管分配。
- a7：外观异常，待质量终验。
- a2：图像闪烁，质量退回补充，并有一条通知终态失败样例。
- a12：采集不连续，已关闭，可查看历史并选择节点重开。

顶部“演示情境与说明”可以直接切换记录，演示 AI 初析生成、模拟下一次生成失败、填入终验示例、模拟收到任务完成结果或主管确认初析变更。模拟外部更新与正常质量操作明确分开。

深链支持 `?record=a8&tab=final`；`tab` 可为 `source`、`assessment`、`analysis`、`tasks`、`evidence`、`final`，以及 `scope` 中文范围。

## 状态、数据与审计

仅浏览器 `localStorage`，键为 `tong-quality-review-20260908-v1`。`events` 保存质量上下文，`formalTasks` 保存单独的正式任务模拟投影。没有业务网络写请求；唯一 `fetch` 用于读取本目录演示日志附件。

质量流程：待初析 → 待主管分配 → 执行/承接/主管验收 → 待质量终验 → 已关闭。终验可退回指定任务；关闭后可重开指定任务。演示重置恢复初始样例。

审计模拟记录：`time/person/title/text`，并在初析版本中保留 `version/author/time/sourceVersion/reason`，退回中保留 `taskId/reason/requirements/time`，终验中保留分类、根因、措施、验证、结论、终验人及时间。通知重试另有公开动态。实际服务端事务、幂等、权限和并发并未在本次原型中迁移或上线。

证据包为可读 TXT 导出，包含任务说明及全部历史证据版本；不是正式系统中的 ZIP 归档接口。

## 早期版本浏览器验证记录（历史）

已实际操作验证：

- 初析必填校验、草稿保存不进入分配、成果增减、完成确认及待分配状态。
- V1/V2 切换、历史初析不覆盖、修订原因、待同步提示、同步前任务保持原要求、同步后重新验收。
- 来源更新确认、原始图片预览、原始文件下载。
- 任务折叠、证据 V1/V2 查看、退回必填、影响链说明、仅指定任务退回，其余有效分支保留。
- 模拟补交新证据与主管验收、终验必填、关闭只读、重开必填、上次关闭记录保留、刷新恢复。
- 补充情况独立保存并转义特殊字符、通知重试、搜索空态、风险筛选。
- 原始附件与证据包已实际下载到本机；证据包包含各任务与版本文本。
- 1366×768：页面 `scrollWidth = clientWidth = 1351`（15px 滚动条）；1920×1080：二者均为 1905。宽屏初析单列输入约 673px，全宽文本框约 1378px；输入字号 14px，单行输入约 46px 高。
- 浏览器未报告页面脚本错误；`node --check tong.js` 与 `node --check source-data.js` 通过。

浏览器工具对 Blob 证据包的下载事件等待超时，但实际文件已生成，并通过文件存在性和内容读取核验；此为工具事件观测差异，不是下载按钮失效。

本次不运行正式业务测试套件：所有代码均为独立静态原型，未改动正式模块。

## AI 初析补齐（2026-09-08）

上一版主要查看了已完成初析的只读页，遗漏了生成草稿的工作区。本次重新查看 8797 运行系统的佟成测试身份，并追踪实际运行的 `.worktrees/yesterday-admin-test-actors` 源码：

- `scripts/local-quality-initial-analysis-dev.ts`：实际运行入口。
- `src/web/quality-tracking-page.ts`（约 318–390 行）：生成按钮、AI 原稿、人工草稿、部门主管匹配、成果选择及推送确认。
- `src/quality/analysis/quality-analysis-prompt.ts` 与 `quality-analysis-contracts.ts`：输出事实、假设、调查方向、部门候选、成果标准；附件只使用元信息和人工说明。
- `src/quality/analysis/quality-analysis-service.ts`：生成尝试独立留存、已保存草稿优先、正式初析版本与任务交接。

此工作区源码比仓库根目录的早期页面和基线说明更新；本次功能以正在运行的源码为依据。原版本文件保存在 `revisions/before-ai-initial-analysis/`，仅存修改前源码副本。

新增交互入口：<http://127.0.0.1:8809/?record=a4&tab=analysis>。

1. 打开或刷新不会自动调用生成；用户点击后展示生成中、成功或失败状态。
2. 首次成功可预填人工草稿。已有草稿时，重新生成只新增原稿，不覆盖人工内容。主动选择重新预填时，替换前草稿留有可查看的只读副本。
3. 每次生成保留来源版本、上游 AI 研判、主管结论、规则/知识/提示词版本、附件人工说明、结果与失败原因；失败也保留记录。
4. 已确认事实与原因假设分开；分析依据注明出处；主管确认分类只读。
5. AI 成果候选默认未勾选，可编辑、删除、添加。选择唯一主责部门后展示唯一有效接收主管，不选择具体执行人。
6. “下一步”先保存草稿，再弹出推送确认。取消后停留草稿；确认才冻结正式 Vn 并展示交接结果。全部是浏览器内的模拟状态，不发送真实通知或创建正式任务。

新增浏览器验证已通过：主动生成及忙碌状态；首稿预填；人工修改不影响 AI 原稿；重新生成保留草稿；失败保留草稿、旧原稿和失败记录；刷新不自动重试；主动重试成功；替换前草稿副本可查；部门/成果/确认说明门禁；取消推送仍为草稿；确认冻结正式版本；旧版本内容不变；仅修改分析不标记任务变更，修改任务要求才标记待主管同步。

1366×768 和 1920×1080 下页面无横向溢出。输入字号 14px，单行输入约 46px；宽屏全宽文本区 1378px，双列文本区各 673px。新增流程浏览器错误日志为空，所有活动 JavaScript 文件通过 `node --check`。

AI 内容为固定的本地交互样例，未连接模型。页面明确标为“交互模拟”，不能据此认定实际模型效果或生产功能已上线。

## 验收与 OA 评论重设计（2026-09-08）

根据用户两张截图及明确要求，新增 `acceptance.js` 和 `acceptance.css`，旧版源码副本保存在 `revisions/before-simple-acceptance/`。本节替代早期终验表单要求；AI 初析、来源与主管任务规划交互保持原有入口。

入口：<http://127.0.0.1:8809/?record=a8&tab=final>。

- 页面仅有“验收通过 / 验收不通过”两个结果选择，移除四项勾选和分类、根因、措施、验证结果等重复填写项。历史结构化记录仍可展开查看。
- 选择通过后，仅显示一个必填“验收意见”输入框，最多 2000 字，内容自动暂存。点击“确认通过并同步评论”才提交。输入框内的换行和文本保留，展示时转义。
- 通过结果在 `closures` 留存，与一条具有稳定 ID 的 `oaComments` 模拟同步记录同时保存。评论显示佟成、时间、事件编号、问题标题和原样验收意见，样式对齐用户截图二的“添加了评论”内容。
- 在验收结果页直接查看评论效果；“来源事实”新增“OA 评论”子页，展示同一组记录。评论另存，不覆盖来源快照或原始审批状态。刷新保留已提交记录。
- 评论同步有模拟的等待、成功、失败状态；失败只重试同一条记录，不重新验收、不重复评论。演示情境中可指定下一次同步失败。
- 不通过时按当前责任链选择一个主管节点，或某位员工的具体任务节点；只填一个“不通过原因与补充要求”文本框。
- 退回主管：当前处理人变为主管，保存待主管处理的退回上下文；员工已有任务结果和证据保持原样，由主管决定后续补充范围。
- 退回员工：只将该节点对应任务变为待补充，其他任务仍保留原验收结果；主管需重新验收补交结果，再送质量验收。
- 重开也支持选择主管或员工节点，并要求原因。历史验收、评论和证据不会删除。

本次浏览器实际验证：意见空白门禁；退回节点与原因门禁；主管退回后 3/3 员工结果仍保留；员工退回只影响一项，其他 2/3 保持通过；退回草稿刷新恢复；再次提交验收；评论文本和换行一致、特殊字符安全显示；评论成功后在来源页可查；失败重试始终一条评论；重开后评论继续保留。浏览器错误日志为空。1366×768 与 1920×1080 布局检查无页面横向溢出。

实际 OA 评论接口、身份授权和单据 ID 映射未接入。本次不向真实钉钉发送评论，不修改正式业务源码或数据库。

## 恢复视角切换（2026-09-09）

右上角恢复“切换视角”按钮，新增 `view-switcher.js` / `view-switcher.css`。菜单提供：

- 新版原型：当前佟成视角、马荣鑫反馈研判原型（8808）。
- 原系统：管理员、马荣鑫（测试）、佟成（测试）、测试主管、测试员工1/2/3（8797）。测试视角使用原系统已有的 `testActor` 链接，不将本地原型数据冒充为原系统记录。

其他视角在新标签页打开；打开前保存当前编辑内容，当前原型页面保留。支持点击外部及 Esc 关闭菜单。原系统仍使用其原有登录会话；会话失效时经过原系统的测试登录页，不绕过身份验证。

8797 原系统使用原来的 `local-quality-initial-analysis-dev.ts --keep-data` 恢复运行，保留已有数据并继续禁用真实同步、回写、通知。8808 马荣鑫静态原型服务同时恢复。恢复前的入口文件保存在 `revisions/before-view-switcher/`。

验证：菜单展开、Esc 关闭、原系统测试主管入口及登录后主管页面、原型导航地址、当前视角标记、浏览器脚本语法检查。


## 2026-09-10 佟成真实事件接入

- 8809 使用 `oa-connected.js` 从 `/api/quality-oa/tong` 读取已正式通报的 OA 事件，不再加载浏览器样例；未通报反馈不会混入初析列表。
- `scripts/quality-oa-workflow.mjs` 复用相邻原系统 `createQualityAnalysisService`，生成 AI、保存人工草稿、正式确认分别使用原质量 SQLite 表。部门及唯一主管取原目录；规划会话使用原数据库旁的 sessions 目录，避免交接写入另一个工作树。
- `/api/quality-oa/tong/{generate,draft,confirm}` 沿用本机授权 Cookie、来源校验和原服务版本冲突校验。原任务与 OA 线上审批不会由列表查询更改；本地通知仍禁用。这是单用户本地接入，非生产身份认证或上线声明。
- 页面保留质量初析的已确认布局；任务进度取正式投影。完整附件、证据和实际终验操作目前通过对应原系统链接查看/操作，不模拟成功，也不声称已在新页完成这些操作的迁移。
- 验证：隔离数据库覆盖 Ma 通报 → Tong 队列 → 草稿保存 → 过期版本拒绝 → 确认进入分配 → 重复确认幂等 → Ma 回显；实际浏览器确认已有 OA 事件进入待初析，真实 AI 生成成功且刷新后原稿仍存在。实际事件没有自动确认或分配。
