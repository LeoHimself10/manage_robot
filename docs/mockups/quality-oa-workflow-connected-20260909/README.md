# 马荣鑫工作台：恢复已确认 HTML 与真实 OA 连接

last_verified_at: 2026-09-09

入口：http://127.0.0.1:8808/ma-workbench/ 。恢复自 `quality-connected-20260909/ma-workbench/`，原稿与此前精简 OA 页均保留。本目录是新的运行版本，沿用原 HTML、CSS、行内来源详情、独立事件页与持续挂载的六阶段导航。

## 确认流程

- 真实 OA 自动同步进入“全部事件”；来源详情仅展示原始表单、附件关联和审批/版本。
- 点击“进入质量事件”并确认才进入待我研判；取消不写入，重复确认不重复入队。后端同样检查准入，不能绕过界面直接生成新 AI。
- AI 与人工分别保存；AI 中的字段代码、内部引用编号与请求信息继续隐藏或转为中文。未引入处理方式选择。
- 保存研判只新增人工版本。确认并推送后复用原系统 `createMaWorkbenchService.submit` 和正式通报事务，进入质量初析。
- 事件页保留处理总览、原始资料、我的研判、佟成初析、分配与承办、验收与动态。马荣鑫只能查看后续结果，未发生的步骤显示待处理，不填入原型模拟记录。
- 正式任务 SQLite 是负责人、执行状态、期限与验收的唯一来源。页面读取原系统投影，不创建第二套任务状态。

## 本机连接与边界

`scripts/quality-oa-workflow.mjs` 将已授权同步的 OA 来源映射至原系统质量来源表；OA 原始版本与 AI 返回继续保存在 `data/quality-oa/oa.sqlite`。映射区分 OA 全量变更版本和原系统资料版本，审批流转不会静默覆盖人工结果。

正式工作流继续使用 `.worktrees/yesterday-admin-test-actors/data/local-quality-initial-analysis-v1/workbench.sqlite`，准入/研判/通报与只读投影复用 `.worktrees/ma-quality-workbench-v1/src/quality/ma-workbench/service.ts`。本机马荣鑫入口对应原验收环境的 `quality-supervisor-local`，不是生产钉钉免登授权。接入前保存了独立 SQLite 备份。

真实 OA 的已确认推送进入 8797 原系统质量初析队列；8809 保留此前佟成 HTML 交互入口，本次未将其模拟业务操作改造成正式初析、任务分配或终验 API。OA 评论写回、线上部署仍不在本次完成范围。

本次没有代替用户确认任何真实反馈准入、人工判断或通报，没有实际发送钉钉消息。真实来源读取与历史 AI 桥接是自动的，人工提交只由用户界面明确触发。原系统本地通知 worker 保持关闭。

## 验证

10 项 Node 回归通过：包含 OA 接入/版本/去重、鉴权/跨站防护、AI 幂等，以及新增的准入门禁、保存不推送、来源更新重新确认、重启保留、正式通报幂等和只读后续投影。

隔离 HTTP 服务 8812 使用明确标识的离线样例，浏览器实际点击验证取消准入、确认准入、AI 采纳、保存、确认推送，以及初析、主管分配、员工承办与验收导航。没有借测试操作真实业务记录。

`scripts/quality-oa-workflow-visual-check.cjs` 在 1366×768、1920×1080 均通过：来源表面无研判/流程导航、事件六阶段持续可点击、推送后研判只读、中文 AI 字段、无横向溢出、无页面 JS 错误。截图与结果保存在忽略目录 `data/quality-oa-ui-qa/`。

### 2026-09-10 视角切换样式统一

last_verified_at: 2026-09-10

马荣鑫 8808 页面移除原生 select，沿用佟成页的 view-switcher 按钮、分组面板和当前视角标记。保留佟成及原任务系统各视角入口；其他视角在新标签页打开。浏览器已验证展开菜单与样式，来源、AI 和业务提交逻辑未改动。

2026-09-10 OA 表单展示修复：完整表单及历史版本递归解析明细行、地址、电话等结构化值，隐藏内部字段标识；链接显示为可点击入口，长文本换行。窄窗口的行内详情宽度适配视口。只改展示，不修改来源快照。已核对实际表单明细，并验证 HTML 转义、链接协议及数值 0 的展示。

2026-09-10 附件显示：增加文件名、大小与按需预览。GET /api/quality-oa/attachment 保留本机会话与来源附件归属校验，仅用服务端文件编号向钉钉申请下载链接。实际附件接口返回 403 AccessTokenPermissionDenied，当前应用仍缺少该接口权限，图片读取尚未验收成功；页面明确展示错误并允许重试，不宣称已能预览。

2026-09-10 附件权限实测更新：用户明确授权后，在钉钉开发平台开通 Workflow.Instance.Write（包含下载附件及审批写能力）。未执行审批变更，也未发布其他应用配置。下载接口随即返回成功；对 OSS HTTP 链接改用 HTTPS，实测 JPEG 返回 200、432123 字节，浏览器已成功显示真实图片。修复预览按钮冒泡触发行点击、导致预览被清空的问题。以上取代此前权限阻塞状态；视频与链接自然过期场景尚未实测。
