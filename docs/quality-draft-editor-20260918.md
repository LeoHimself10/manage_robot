# 草案编辑可读性 r18

last_verified_at: 2026-09-18

用户要求执行动作、前置依赖可不填，并解决多处显示不全。两字段后端本来可空；本次只改编辑界面，默认收起并标注选填，已有内容通过计数提示，展开可修改，提交仍读取全部字段防止丢值。任务名称改为自动增高的多行输入，主要列加宽，字体 14px，弹窗扩大到 96vw / 94vh；任务标题多行显示，任务背景独立展开编辑。窗口、全屏和列宽变化重新计算文本高度。

验证：类型检查通过；草案转换、编辑、修订相关 10 项测试通过（含可选字段留空）；Playwright 使用真实浏览器 bundle 和 CSS、模拟 API，在 1366×768 / 1920×1080 验证文本高度、选填列展开收起、提交保留已有值。未用测试保存操作改动用户真实草稿。

发布范围：仅 manage-robot-quality-cao 独立测试容器，镜像 manage-robot:quality-test-draft-editor-20260918-r18b。原正式任务执行流程和数据不修改。
备份：/opt/quality-pilot/backups/draft-editor-r18-20260918。
回滚：以 manage-robot:quality-test-list-compact-20260918-r17 和原 env、挂载、端口重建。

补充：静态脚本 URL 增加新版标记，避免浏览器继续使用缓存的旧编辑器。r18b 备份目录为 /opt/quality-pilot/backups/draft-editor-r18b-20260918；最初 r18 备份保留。

上线核验完成：r18b 健康检查、534 个文件哈希及访问门禁通过，正式任务、质量事件、初析、证据与审计校验无变化。
