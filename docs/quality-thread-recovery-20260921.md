# 质量分配入口及整表规划恢复 r23

last_verified_at: 2026-09-21

现场记录表明 15:14 多个会话经删除接口移除，质量移交仍指向这些 threadId。另一次完整方案请求被分类器归为 split，工具仅开放 split_draft_task，不能自由生成整套草案。

最新待规划移交的接收主管重新打开缺失侧会话时，从权威 task_package_json 恢复原 planId/threadId 的待规划草稿和一条恢复说明。保留承接关系，不恢复被删历史，不覆盖尚存草稿，不恢复已发布/已关闭/旧初析，其他用户无法恢复。会话事件日志记录恢复。未完成关联会话删除接口返回409与明确说明；普通侧会话删除不变。

完整方案按钮携带明确动作标识，质量上下文下强制 replace_draft，跳过单行拆分分类器；整表重试规则阻断拆分补跑。成果覆盖及人工确认门禁继续有效，生成草案不代表已分配。

验证：类型及内联脚本检查通过，15项针对恢复/界面测试及58项会话解析、工具路由、回合契约回归通过。未代用户调用模型生成或发放任务。
部署 manage-robot-quality-cao，镜像 manage-robot:quality-test-thread-recovery-20260921-r23。
备份 /opt/quality-pilot/backups/thread-recovery-r23-20260921。
回滚镜像 manage-robot:quality-test-plan-coverage-20260921-r22，保留原 env/挂载/端口。

已上线：535 个文件哈希、健康与访问门禁通过，受保护业务表保持一致。使用实际失效移交在容器临时会话目录验证恢复与幂等，任务包保持一致、越权拒绝；未写真实用户会话。用户从原入口重新打开时按需恢复。
