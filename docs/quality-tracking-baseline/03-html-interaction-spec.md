# 测试入口交互基线

## 主管列表精简 r17（2026-09-18）

未正式分派的事项仅显示主管及状态，不在员工／分配事项列重复展开初析和来源正文。正式任务名称最多两行，每个事件预览两项，其余点击事件查看；完整详情及业务数据保留。last_verified_at: 2026-09-18


## 主管先承接再分配 r16（2026-09-18）

未发放的初析移交先进入“待我承接”，由指定主管确认承接后进入“待分派员工”。旧草稿保留，已发放/关闭事项不补造承接。旧规划链接在承接前转回主管工作台，编辑、AI 规划与发布后端均有门禁。承接记录与真实操作人审计独立追加，不复制正式任务状态。质量总览的时段“终验关闭”指标文案改为“流程完成”，计数口径不变。详见 [r16 记录](../quality-manager-acceptance-20260918.md)。

## 原始研判一致性修复 r15（2026-09-18）

OA 研判明确处理方式与分类的双向配对规则，仅在单一配对冲突时允许模型修正一次，随后仍执行完整校验。其他校验失败与网络错误不自动重试，默认非 OA 调用仍只调用一次。提示区分服务失败与结果校验失败，错误记录不包含密钥、来源正文或模型原文。详见 [r15 修复记录](../quality-assessment-fix-20260918.md)。

## 岗位配置与成果任务映射 r14（2026-09-18）

以本节作为本次改造后的补充基线；下文 r13 及更早记录保留为历史。业务操作使用“客服主管”“质量主管”，实际身份保留在审计。管理员通过 `/admin/posts.html` 配置真实岗位与测试岗位，每岗一人，版本校验防止覆盖他人修改。换人后即时撤销旧岗位权限并接续未完成工作；不改写已有部门任务及历史数据。

初析的必须成果与执行任务分离。新移交只建立待规划占位项，主管可让 AI 生成或手工增删、拆分、合并任务；每项成果须明确一个最终交付任务和可选支撑任务，同一任务可交付多项成果。人工确认结构后才能选人、发放；一项任务也可成立。结构变更使确认失效，日期与人员变更沿用原流程。详见 [r14 实施和验证记录](../quality-roles-planning-20260918.md)。


## 主管移交可见性 r13 修复（2026-09-17）

独立质量试点已发布 r13：最新初析移交给主管但尚未生成正式任务时，主管列表及详情可见，并提供进入原任务分配侧会话的入口。仅接收主管获得该可见性；不新增分派状态、不改通知开关、不迁移数据库。两条受影响事件已在线上投影核验恢复，详见[修复记录](../quality-handoff-fix-20260917.md)。


last_verified_at: 2026-09-18

马荣鑫、佟成页面维持确认版布局；所有视角在当前页打开。顶部提供马荣鑫、佟成、模拟主管、模拟员工1/2/3入口和隔离测试提示。部门下拉框仅包含“模拟测试部门”，唯一对应“模拟主管”。主管/员工使用各自虚构业务身份，真实登录操作人另留审计。返回马荣鑫/佟成清除模拟身份选择。

只在独立测试数据卷运行；不发送消息、待办或催办，不回写 OA。旧初析草稿的真实部门不会自动改为模拟部门，应重新选择后保存。原始来源和 AI 快照不修改。原正式任务页面和处理逻辑复用。

源码：scripts/quality-simulation.mjs、scripts/quality-production-server.mjs、public/quality/{ma-workbench,tong}/view-switcher.js、src/web/quality-tracking-page.ts。

2026-09-14 r5 修复：模拟主管和员工必须显式解析角色视角，页面 main 为 qpc-unified，员工标题“我的质量任务”、主管标题“主管质量工作台”；同时启用对应的事件投影 API，不能因模拟身份不是 admin 回退为售后默认界面。

历史 r6（已由 r7 页内办理替代）：员工责任链卡片按 quality_task_links 关联原正式子任务；已有节点也必须显示处理入口。待承接进入原员工待承接列表（当前页），其他状态进入执行或历史列表；卡片状态、截止和承接时间读取正式任务，禁止显示滞后的节点状态。

2026-09-14 r7：按用户确认的员工页内处理 V3 原型实现。质量事件第四步直接显示正式任务目标、交付成果、期限、验收标准、必交证据、进度与完成说明。每项必交成果直接添加文件，不再显示重复分类下拉框；选交补充材料独立展示。承接、拒绝、保存进度、保存草稿、上传、预览、新版本与提交主管验收均留在当前页面。主管退回显示意见，已提交版本不可删除；再次提交后只读，主管通过不等于质量事件关闭。不提供阻塞/主管协助按钮，不支持视频。

必交项由正式任务 deliverables 生成，支持 JSON 字符串数组、换行和分号列表；不会把原型里的两个示例成果写入任务。自由文本验收标准完整展示，由主管核验文件内容。未填写 deliverables 时沿用至少一份完成证据规则，不推测或编造细项。

验证：1366×768、1920×1080 无横向溢出；正文 14px、按钮至少 42px。已检查缺项提示、主管通过后的只读界面及受控证据预览。

2026-09-14 r8：主管在每张员工任务卡片内直接验收通过或退回补充，移除“去原主管任务验收”及重复跳转提示。通过可填验收意见；退回展开当前员工和具体任务名称，原因与补充要求必填，确认后仅该任务恢复执行。未提交、已通过、已退回和只读视角不显示可写验收表单。员工补交后显示待主管验收，历史退回意见不覆盖当前状态。最后一项通过继续调用原主管验收链路，自动送质量终验。

历史确认的终验退回目标为主管节点或员工的具体任务；质量管理节点下拉框增加任务名称和空白选择项，避免同一个员工多项任务时选错。主管逐项验收权限与佟成终验权限分开。

r8 浏览器验证：1366×768、1920×1080 无水平溢出；正文 14px，操作按钮 44px；退回目标、空原因阻止提交、取消退回和受控证据入口正常。浏览器走只读代理；写入链路在专用服务器副本内通过 HTTP 验证。

2026-09-14 r9：主管合并为“任务分配与验收”，移除重复的证据验收页签，保留只读质量终验结果。佟成在当前页查看任务要求、证据（含 PDF 分页预览）、主管验收后，填写单一意见并通过关闭或选择主管／具体员工任务退回。主管退回保留所有员工成果，由主管补充说明再次送终验，或选择自己已通过的员工任务补充；员工退回仅重开指定任务，提交后必须重新经过主管验收。终验／退回意见不会因刷新状态被伪造为已同步，测试模式显式显示未写入 OA。
