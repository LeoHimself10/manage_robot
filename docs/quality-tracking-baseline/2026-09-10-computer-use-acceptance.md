# 本地质量流程验收清单

last_verified_at: 2026-09-10

## 实测结果

本地专用事件 `QE-20260910-E2EC5AEA`（来源 `LOCAL-QA-20260910-01`）已在 18:00 完成终验关闭。正式任务 `TASK-20260910-0001` 下有 4 项子任务。所有角色使用原系统同一 SQLite；来源入口使用本地构造的明确标注测试记录，并非向钉钉线上提交新 OA。

本轮使用 Computer Use 操作页面；AI 研判与初析手动触发真实模型，业务提交使用原服务。钉钉通知和 OA 写回在本地关闭，未验证线上消息实际送达。

## 浏览器实测通过

- [x] 本地来源进入马荣鑫待研判；来源和人工研判分别留存。
- [x] 马荣鑫生成 AI、保存人工结论、显式确认通报；保存不等于推送。
- [x] 佟成收到同一事件，生成初析、保存草稿、选择研发部及必须成果、正式确认。
- [x] 研发主管收到原智能规划会话；真实 AI 拆为 4 项，逐项选员工，配置依赖、期限和验收要求。
- [x] 预检后正式发布，员工收到 4 项任务；承接进入执行中。
- [x] 无证据时提交被阻止；上传测试文件后可以提交主管验收。
- [x] 主管退回第一项，员工补交 V2，再次验收；V1 仍保留。
- [x] 四项逐项通过，主管汇总证据包并整体送质量终验。
- [x] 质量管理人员填写结论并关闭，关闭审计可见。
- [x] 马荣鑫、佟成页面均显示已关闭；马荣鑫可查看 4 项正式任务及最新关闭动态。
- [x] 佟成列表、详情在 1366×768 和 1920×1080 下进行视觉检查，无横向挤压或关键按钮遮挡。

## 本轮修复

1. 前置任务 `task_1` 被错误拆成 `ta、k_1`：修正服务端模板中的正则转义，并执行生成后浏览器脚本的回归测试。
2. 佟成部门列表与原系统不一致：本地启动器输出非敏感角色路径配置，UI 服务复用原主管目录及数据库。
3. 主管接到初析但尚未发布时，待分配投影遗漏：按接收主管查询交接记录；其他主管不可见。
4. 质量事件读取失败仍停留“正在加载”：增加明确失败状态。
5. 非投影视图的主管研判页签为空：补充已冻结的主管结论与无记录状态。
6. AI 完成标准把质量关闭作为员工前置条件：修正本次草案，补充后续拆解提示词约束。提示词约束不等于绝对保证，主管仍需核对。
7. 无证据提交误报服务故障：映射为明确的业务校验提示。
8. 主管退回使用原生 prompt，内嵌浏览器无反馈：改为页面内必填原因弹窗，同步用于节点驳回与分支退回。
9. 开启测试身份后，正常质量通知名单混入测试专员导致整体验收回滚：隔离通知名单，保留测试专员独立能力与边界校验。
10. 员工卡片不显示主管退回意见：读取本人退回节点的最新意见；事件长背景折叠以减少重复。数据读取和越权情况有回归覆盖。
11. 佟成“最近更新”取最早动态：改为正式事件及公开动态的最新时间。
12. 已关闭事件仍显示“尚未进入终验”：改为关闭结果说明。
13. 未选择的候选成果混入“必须提交成果”：正式版本只展示已选择成果，草稿仍保留候选项。
14. 马荣鑫“最新动态”显示最早五条：改为时间倒序取最新五条。
15. 佟成初次读取先显示空列表：增加读取中提示；补充事件页签深链恢复。
16. 真实连接页面的部门预览与交接状态包含演示字样、内部枚举：改为业务中文。

## 回归证据

原服务工作树：`yesterday-admin-test-actors`。8 个测试文件、73 项通过，覆盖质量能力、页面、角色 HTTP、隔离、任务投影、流程及角色指标。TypeScript 检查通过。两个连接页面 JavaScript 语法检查通过。

新增回归重点：依赖 ID 转义、测试专员不混入正常通知名单、未发布交接的主管待办、退回意见仅本人可见且通过后不残留。

## 边界及后续检查

- 本次不是线上 OA 提交流程验收：真实企业审批回调、新 OA 自动拉取、钉钉评论写回、通知送达需在获准的线上测试单上另验。
- 终验实际操作仍通过原系统页面完成，新佟成页面提供入口和结果。新页面完整附件、证据、终验表单迁移尚未完成，不能描述为全部已迁移。
- 质量终验退回指定节点、关闭后重开、多级跨部门分派及并发冲突有部分自动化覆盖，本轮没有逐个通过浏览器走完所有组合。
- 本轮不宣称系统零缺陷。旧原任务页面仍有技术账号显示、部分动态名称较笼统、终验页较长等可继续优化项。
- 源码与本地服务已更新；本轮未提交或推送 GitHub，未部署线上。
