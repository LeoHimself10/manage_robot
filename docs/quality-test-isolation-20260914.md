# 质量追踪测试隔离

last_verified_at: 2026-09-14

用户明确当前是测试系统，不允许给真实人员发送消息。此前独立容器与正式任务服务共享数据库，仅关闭本进程通知不足以隔离后台消费。

## 当前部署

- 镜像：manage-robot:quality-test-isolated-20260914-r3；容器仍为 manage-robot-quality-cao；端口和钉钉入口不变。
- env：/opt/quality-pilot/test-app.env（服务器私有文件，不入库）。
- 数据卷：/opt/quality-pilot/test-data-20260914 → /app/data；不再挂载 /opt/manage_robot/data。正式系统仍使用原数据卷。
- 使用 SQLite backup 生成一致性副本，保留已有测试进度、OA 缓存和会话。部门/主管仍是通讯录快照，不能描述为虚构账号；视角切换仍使用曹玉寒真实认证身份，业务写入仅在测试副本。此项为 r3 历史状态；r4 模拟身份实现见下。
- 钉钉认证、OA 读取及 AI 分析仍可使用；业务通知、主管通知、质量通知 worker、催办、通讯录同步、来源回写关闭。关闭不代表模拟发送成功。
- 服务强制 QUALITY_PILOT_TEST_MODE=1 和 /app/data/.quality-test-isolated 标记，启动时缺任一项即失败；页面显示测试隔离提示。

## 原共享库的既有记录

- 核查到本入口 1 条 PENDING_ANALYSIS 事件、0 条正式任务；该事件无任务桥接。
- 关联的 1 条 PENDING 通知和 1 条 PENDING 来源回写改为 DEAD，last_error 记录 TEST_ISOLATION_20260914 原因，保留历史。检查时均未发送，不代表对全部历史消息做过全面审计。
- 修改前备份：/opt/quality-pilot/backups/before-test-isolation-20260914.sqlite；停用记录：/opt/quality-pilot/test-isolation-20260914.json。不得恢复整个正式库覆盖正常工作。

## 验证

- 22 项身份与导航测试通过。
- node --import tsx scripts/quality-test-notification-safety.test.mjs：覆盖 10 个实际通知方法，包含消息卡片、发布、改派、停办、主管/员工反馈、逾期、催办、进展及钉钉待办；均 enabled=false，注入网络 mock 调用次数为 0。未向真实人员发送测试消息。
- 镜像构建 518 个文件校验通过；隔离模式缺失、隔离卷缺失两种容器启动均拒绝。
- 当前 /health 正常，公网登录页出现测试提示；正式系统、明思、安徽和 Caddy 容器未重启。

## 运维约束

后续只使用 test-app.env 与测试数据卷，禁止按历史 new-app.env + 正式数据卷的命令回滚。服务当前仍为认证后的单用户测试入口；本次未新增模拟身份授权或开放其他人员访问。

## r4 模拟部门与执行身份

last_verified_at: 2026-09-14

- 新版本 quality-test-simulation-20260914-r4 在独立测试库启动时将真实通讯录联系人停用（保留历史），只启用模拟测试部门、模拟主管及模拟员工1/2/3。虚构 userId 为 QUALITY_SIM_MANAGER 与 QUALITY_SIM_EMPLOYEE_1/2/3，unionId 为空；员工资料目录和种子列表也切到独立空目录，避免真实候选兜底。
- 马荣鑫/佟成继续由已验证的实际登录用户进行研判/初析，主管和员工页面在通过同一外层钉钉认证后，使用服务器限定的模拟业务身份。真实认证 Cookie 不被模拟会话覆盖；下游通过既有 impersonation 上下文记录实际操作人。模拟会话不可用作外层认证，不开放任意真实用户代办。
- 现有正式任务 UI、发布、承接和验收机制在测试库复用。页面顶部提供马荣鑫、佟成、模拟主管、模拟员工1/2/3六个当前页切换入口。主管、员工原菜单也保持可切换；返回马荣鑫/佟成清除模拟选择。
- 模拟身份只在 QUALITY_PILOT_TEST_MODE=1 时获得质量页面访问能力；不授予质量研判、初析或终验能力。通知和 OA 回写继续禁用，数据卷和 env 沿用 r3 隔离配置。
- 原有初析草稿的真实部门不会自动替换，应在界面重新选择模拟测试部门后保存。历史来源和 AI 快照不修改。
- 校验：3项模拟目录/身份安全测试、20项质量权限/页面测试、22项原认证/导航测试、TypeScript 检查通过；10个通知入口继续为零网络调用。隔离预发布验证真实 HTTP 下的4个业务身份、佟成 API 唯一模拟部门、模拟主管交接、员工指派、SQLite发布与员工承接，并拒绝其他模拟员工越权承接。预发布数据不导入用户测试库。

已更新用户测试入口容器至 r4；运行中的519个文件校验通过，数据库活动联系人严格为4个模拟账号且只属于模拟测试部门。更新前测试库备份为 /opt/quality-pilot/backups/before-simulation-r4-20260914.sqlite。预发布容器已停止，验收事件未进入用户测试数据卷。

## r5 模拟身份新版页面修复

last_verified_at: 2026-09-14

原因：r4 只验证模拟身份和业务操作，漏验实际页面视角；quality-http 对非 admin 模拟用户未生成 pagePerspective，导致员工回退售后默认旧界面。r5 为受限模拟身份解析员工/主管视角，并让页面和投影 API使用同一身份。员工恢复“我的质量任务”，主管恢复“主管质量工作台”，加载原确认版 QUALITY_UNIFIED_STYLES，不改任务数据。

新增 HTTP 回归覆盖角色面板开关两种配置、主管及三名员工的统一布局 class、实际视角、投影标记和标题。31项页面/API测试与TypeScript检查通过。隔离预发布四个页面和投影API均通过；通过应用内浏览器实际查看员工和主管新版截图及任务列表。浏览器验证用只读预发布代理、专用测试密钥，未伪造真实登录。已部署 manage-robot:quality-test-unified-ui-20260914-r5，继续使用 test-app.env 和独立测试数据卷。

## r6 员工承接入口

last_verified_at: 2026-09-14

已有责任节点时，投影分支漏掉 taskUrl，并使用节点旧状态。修复为按正式任务关联补齐链接和状态；“去承接任务”在当前页打开原员工待承接列表，沿用原承接 API。增加已有节点、状态变化和其他员工无权查看的回归。32项测试及 TypeScript 检查通过。通知禁用和独立测试卷不变。
预发布使用用户测试库的独立副本验证：实际浏览器点击“去承接任务”在同一标签进入原员工页并显示“接受”；HTTP 验证其他员工被拒绝、本员工承接成功、状态回传执行中。副本操作未回写用户测试任务。已更新独立测试入口至 manage-robot:quality-test-employee-accept-20260914-r6。
