/** Re-export for eval scripts — canonical impl in src/agent/eval/assistant-quality.ts */
export {
  assertAssistantMessageQuality,
  assertPoolFileNotesCoverage,
  assertAssigneesFromPool,
  assertDistinctAssigneeCount,
  type AssistantQualityOpts,
} from "../src/agent/eval/assistant-quality";
